# AGENTS.md - iw-mixes-app-wx 微信小程序项目指南

## 项目定位

`iw-mixes-app-wx` 是 IW 系统当前唯一默认微信小程序迭代项目。用户说 `wx项目`、`前端微信小程序`、`微信小程序`、`小程序` 或 `移动端` 时，默认进入本项目。

历史来源项目 `../iw-mixes-app` 已归档，只在需要追溯旧 uni-app 实现、比对页面逻辑或补齐迁移细节时读取。新页面、新交互、新接口调用都应落在本项目，后端接口以 `../iw-mixes-server` 为准。

## 技术约束

- 只使用原生微信小程序文件：`.wxml`、`.js`、`.wxss`、`.json`。
- 不引入 Vue、Pinia、uni-app、`uni_modules`、`uni.*` API 或 Vue 模板语法。
- UI 优先使用 WeUI。WeUI 无法覆盖的业务交互，再在 `components` 下自建最小组件。
- 网络、缓存、路由、上传、弹窗等能力使用 `wx.*`。

## 目录地图

- `app.json`：页面、分包、tabBar 和全局窗口配置。
- `app.js`：全局启动逻辑，负责登录态、字典缓存和家庭组预加载。
- `app.wxss`：全局样式，包含 WeUI 样式入口。
- `config/env.js`：运行环境识别和接口地址切换。
- `api`：请求封装和业务 API。
- `stores`：原生小程序轻量状态与本地缓存封装。
- `pages`：主包页面。
- `pagesAuth`：登录认证页面。
- `pagesBase`、`pagesBookkeeping`、`pagesEat`、`pagesPoints`：业务分包。
- `static`：从原小程序端迁移来的图片、字体和图标资源。
- `scripts`：迁移辅助脚本。

## 接口环境

开发版默认走后端本地兼容入口：

```js
http://localhost:18000
```

发布版自动走生产 API 入口：

```js
https://api.itwray.com
```

本地 `18000` 由 `iw-mixes-server` 的 `iw-core` dev profile 兼容旧 `/auth-service`、`/bookkeeping-service`、`/eat-service`、`/points-service` 和普通 `/external-service` HTTP 请求。发布版由生产 Nginx 兼容旧入口前缀。

如需临时切换环境，使用 `config/env.js` 暴露的 `setRuntimeEnv`，不要在页面里硬编码域名。

## 开发与迁移流程

1. 只有在需要对照旧实现时，才从 `../iw-mixes-app` 读取对应页面和业务 API。
2. 先确认页面真实路径已在 `app.json` 注册。
3. 将 Vue 模板转换为 WXML：`v-if` -> `wx:if`、`v-for` -> `wx:for`、`v-model` -> `value + bindinput`。
4. 将 `<script setup>` 状态与方法转换为 `Page({ data, onLoad, onShow, ... })`。
5. 将 `uni.*` 调用替换为 `wx.*` 或 `api/request.js` 封装。
6. 将 `uni-ui/lime-echart` 组件替换为 WeUI、自建组件或微信小程序生态组件。
7. 最后运行 `npm run check:routes`，并在微信开发者工具中构建 npm、编译页面。

## 默认开发标准

- 新功能默认只改本项目，除非用户明确要求联动后端、Web 或归档小程序。
- 业务接口、字段含义、枚举取值优先从本项目已有页面、`api` 封装、`stores`、旧 uni-app 对照页和后端 Controller/DTO/VO 确认，不凭感觉发明字段或路径。
- 后端服务代码默认不改；如果现有接口能满足需求，优先通过请求参数、前端适配、状态处理和样式重构完成。
- 页面实现顺序建议为：确认路径和入口 -> 对照已有或旧版页面 -> 确认接口数据 -> 实现结构和数据 -> 补齐样式 -> 处理交互和异常态 -> 自验。
- 新增页面必须注册到 `app.json` 对应主包或分包；新增入口时同步检查菜单、tabBar、按钮跳转或其他来源页面。
- 公共样式、公共 helper 或组件改动要先评估影响面；单业务页面能闭环时，优先把改动收敛在对应业务目录。
- WeUI 只作为基础组件优先选项。若 WeUI 默认样式导致布局、间距、弹框、按钮或输入框无法满足业务体验，应手写最小原生 WXML/WXSS 组件。
- 复杂交互先保证业务流程和状态正确，再做视觉优化；不要为了视觉重构改变既有接口语义或核心操作路径。

## 默认 UI 规范

- 移动端工具型页面应紧凑、稳定、可扫读，避免营销式大卡片堆叠。
- 固定格式控件要给稳定尺寸，例如键盘格、tab、筛选栏、统计卡、图表 canvas，避免内容变化造成布局跳动。
- 页面底部操作区要考虑安全区，不出现无意义空白或按钮贴边。
- 重要金额、数量、状态和按钮文字不得换行，必要时设置 `white-space: nowrap`。
- 长数字、长备注、长来源字段、长名称等内容必须允许断行，常用 `word-break: break-all` 和 `overflow-wrap: anywhere`，不能遮挡右侧金额或操作按钮。
- 输入框、日期选择和短按钮同一行时，默认让输入框/日期框 `flex: 1`，短按钮 `flex: 0 0 auto`，按钮只按文字占用紧凑宽度。
- 横向筛选行里的“重置”“筛选”“确认”等短按钮，如果原生 `button` 过高、过宽或文本不居中，应改用 `view` 手写紧凑按钮；常用高度 `56rpx`，左右内边距 `20rpx` 到 `24rpx`。
- 按钮文字必须水平和垂直居中。弹窗按钮受 WeUI 默认样式影响时，应手写弹窗操作区。
- 需要阻止遮罩点击冒泡时，弹窗按钮优先使用 `catchtap`。
- 弹窗高度应随内容自适应，同时设置最大高度和滚动，避免内容过多撑出屏幕。
- 编辑类弹窗可先用当前列表数据立即打开，再异步拉详情刷新，避免接口慢时表现为“点击没反应”。
- 列表项标题、分类、时间、摘要之间保留少量清晰间距即可；金额列靠右且不换行。
- 详情卡片和列表项不要嵌套过多卡片，必要时使用分隔线和紧凑间距。
- 统计图在原生小程序内优先使用 canvas 手写轻量图表，避免为单页引入过重依赖。
- 空数据文案只保留一处，避免 canvas 和外层 DOM 重复显示。

## 默认验证清单

- 常规改动至少运行 `npm run check:routes`。
- 改动页面 JS 时，优先对目标脚本运行 `node --check <target-page>.js`。
- 涉及 UI、弹窗、图表或复杂交互时，默认用微信开发者工具编译目标页面；可使用 `cli preview --compile-condition` 指定页面路径。
- 用户要求“自行验证”时，微信小程序改动默认由 AI 自行打开或使用微信开发者工具完成模拟器编译，并验证目标 UI；优先使用微信开发者工具 CLI `preview` 和可用的模拟器自动化/截图检查，不能只要求用户手动打开验证。
- UI 自验重点检查：页面能打开、关键弹窗能出现、按钮居中、横向工具栏比例合理、长文本不遮挡金额或按钮、底部操作区适配安全区、空状态不重复。
- 最终说明写清楚改动文件、验证命令和微信开发者工具无法覆盖的风险。

## 迁移经验文档

- 记账模块迁移和精细化 UI 优化经验已沉淀在 `docs/bookkeeping-migration-experience.md`。
- 本文档已抽取其中的通用开发标准和 UI 规范作为默认要求；如果用户提到“参考记账迁移经验”“参考经验文档”，或需求涉及餐饮、积分任务、基础功能等页面的精细 UI 优化，应先阅读该文档，再按其中更细的模块经验实施。

## 注意事项

- 不要修改 `node_modules/`、`miniprogram_npm/`、构建缓存或真实私有配置。
- 不要把真实 appid 写入公共配置；需要时使用 `project.private.config.json`。
- 不要猜接口字段含义，优先查本项目已有原生页面、`api`、`stores` 和 `../iw-mixes-server` 后端 Controller。
- 只有需要旧实现对照时才查 `../iw-mixes-app` 页面和 API，不要默认修改归档项目。
- 如果迁移中遇到字段语义、交互取舍或组件替换不确定，先问用户。
